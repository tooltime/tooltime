<div class="container">
  <div class="row">
    <div class="span12">
      <h1>Your data is now being processed.</h1>
      <p>Please be patient, processing the data may take several minutes.</p>
    </div>
  </div>
  <%= form_tag(:style => 'display: none;') do %>
    <%= hidden_field_tag "job_id", @job_id, { :id => "job-id" } %>
  <% end %>
</div>
